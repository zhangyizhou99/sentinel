# 运行时依赖，尽量精简。openai 与 python-dotenv 都是可选增强：
# 没装 openai 也能跑（离线降级）；没装 dotenv 也能读系统环境变量。
python-dotenv>=1.0
openai>=1.0

# 外部 MCP Client（v1 是当前稳定线；v2 发布前避免自动跨主版本升级）。
mcp>=1.27,<2

# 多语言扫描（可选）：装了才能扫 Python 以外的语言（JS/TS/Go/...）。
# 没装时仅 Python 可扫，其它语言在 check_language_support 里显示为 extendable。
# tree-sitter-language-pack>=0.9

# 开发/测试
pytest>=7.0
